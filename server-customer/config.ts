
class Customer {
    static readonly config = {
        "允许下载docx":true,
        "允许下载sqlite":true,
    };
}

export default Customer;


export const enum serverPrefix {
  value = "/pwweb-server-c"
}